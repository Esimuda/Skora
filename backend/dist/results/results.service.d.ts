import { Repository } from 'typeorm';
import { ClassResult } from './class-result.entity';
import { SubmitResultDto } from './dto/submit-result.dto';
import { ApproveResultDto } from './dto/approve-result.dto';
import { RejectResultDto } from './dto/reject-result.dto';
import { ClassesService } from '../classes/classes.service';
import { StudentsService } from '../students/students.service';
import { SubjectsService } from '../subjects/subjects.service';
import { ScoresService } from '../scores/scores.service';
import { PsychometricService } from '../psychometric/psychometric.service';
import { CommentsService } from '../comments/comments.service';
import { NotificationsService } from '../notifications/notifications.service';
import { TeachersService } from '../teachers/teachers.service';
import { MailService } from '../mail/mail.service';
import { UsersService } from '../users/users.service';
export declare class ResultsService {
    private repo;
    private classes;
    private students;
    private subjects;
    private scores;
    private psychometric;
    private comments;
    private notifications;
    private teachers;
    private mail;
    private users;
    constructor(repo: Repository<ClassResult>, classes: ClassesService, students: StudentsService, subjects: SubjectsService, scores: ScoresService, psychometric: PsychometricService, comments: CommentsService, notifications: NotificationsService, teachers: TeachersService, mail: MailService, users: UsersService);
    submit(schoolId: string, dto: SubmitResultDto, teacherUser: any): Promise<ClassResult>;
    findAll(schoolId: string, status?: string, term?: string, academicYear?: string): Promise<ClassResult[]>;
    findOne(schoolId: string, classId: string, term: string, academicYear: string): Promise<ClassResult>;
    approve(schoolId: string, id: string, dto: ApproveResultDto, principalUser: any): Promise<ClassResult | null>;
    reject(schoolId: string, id: string, dto: RejectResultDto, principalUser: any): Promise<ClassResult | null>;
    getComputedResults(schoolId: string, classId: string, term: string, academicYear: string): Promise<{
        position: number;
        totalStudents: number;
        classHighest: number;
        classAverage: number;
        term: string;
        academicYear: string;
        student: import("../students/student.entity").Student;
        scores: import("../scores/score.entity").Score[];
        psychometricAssessment: import("../psychometric/psychometric-assessment.entity").PsychometricAssessment | null;
        comment: import("../comments/comment.entity").ResultComment | null;
        totalScore: number;
        totalPossible: number;
        percentage: number;
    }[]>;
}
